apiVersion: v1
kind: ConfigMap
metadata:
  name: shipping-service-config
  namespace: ecommerce-prod
  labels:
    app: shipping-service
    app.kubernetes.io/part-of: ecommerce
data:
  ASPNETCORE_ENVIRONMENT: Production
  RabbitMQ__Host: rabbitmq
  RabbitMQ__Port: "5672"
  RabbitMQ__Username: ecommerce
  FedEx__BaseUrl: https://apis.fedex.com
  Jaeger__Endpoint: http://jaeger:4318/v1/traces
  Serilog__MinimumLevel__Default: Information
---
# Replace all stringData values before deploying to a real cluster.
apiVersion: v1
kind: Secret
metadata:
  name: shipping-service-secret
  namespace: ecommerce-prod
  labels:
    app: shipping-service
    app.kubernetes.io/part-of: ecommerce
type: Opaque
stringData:
  ConnectionStrings__ShippingDb: "Host=postgres;Port=5432;Database=shipping_db;Username=ecommerce;Password=changeme"
  RabbitMQ__Password: "changeme"
  FedEx__ApiKey: "replace_me"
  FedEx__WebhookSecret: "replace_me"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: ecommerce-prod
  labels:
    app: shipping-service
    app.kubernetes.io/name: shipping-service
    app.kubernetes.io/part-of: ecommerce
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
        app.kubernetes.io/name: shipping-service
        app.kubernetes.io/part-of: ecommerce
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: shipping-service
                topologyKey: kubernetes.io/hostname
      containers:
        - name: shipping-service
          image: REGISTRY/shipping-service:IMAGE_TAG
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: shipping-service-config
            - secretRef:
                name: shipping-service-secret
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health/live
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
            periodSeconds: 15
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: ecommerce-prod
  labels:
    app: shipping-service
    app.kubernetes.io/part-of: ecommerce
spec:
  selector:
    app: shipping-service
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: shipping-service-hpa
  namespace: ecommerce-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: shipping-service
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
