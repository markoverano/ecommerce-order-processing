apiVersion: v1
kind: ConfigMap
metadata:
  name: saga-orchestrator-config
  namespace: ecommerce-prod
  labels:
    app: saga-orchestrator
    app.kubernetes.io/part-of: ecommerce
data:
  ASPNETCORE_ENVIRONMENT: Production
  RabbitMQ__Host: rabbitmq
  RabbitMQ__Port: "5672"
  RabbitMQ__Username: ecommerce
  Jaeger__Endpoint: http://jaeger:4318/v1/traces
  Serilog__MinimumLevel__Default: Information
---
# Replace all stringData values before deploying to a real cluster.
apiVersion: v1
kind: Secret
metadata:
  name: saga-orchestrator-secret
  namespace: ecommerce-prod
  labels:
    app: saga-orchestrator
    app.kubernetes.io/part-of: ecommerce
type: Opaque
stringData:
  ConnectionStrings__SagaDb: "Host=postgres;Port=5432;Database=saga_db;Username=ecommerce;Password=changeme"
  RabbitMQ__Password: "changeme"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: saga-orchestrator
  namespace: ecommerce-prod
  labels:
    app: saga-orchestrator
    app.kubernetes.io/name: saga-orchestrator
    app.kubernetes.io/part-of: ecommerce
spec:
  replicas: 3
  selector:
    matchLabels:
      app: saga-orchestrator
  template:
    metadata:
      labels:
        app: saga-orchestrator
        app.kubernetes.io/name: saga-orchestrator
        app.kubernetes.io/part-of: ecommerce
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: saga-orchestrator
                topologyKey: kubernetes.io/hostname
      containers:
        - name: saga-orchestrator
          image: REGISTRY/saga-orchestrator:IMAGE_TAG
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: saga-orchestrator-config
            - secretRef:
                name: saga-orchestrator-secret
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /health/live
              port: 8080
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
            periodSeconds: 15
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: saga-orchestrator
  namespace: ecommerce-prod
  labels:
    app: saga-orchestrator
    app.kubernetes.io/part-of: ecommerce
spec:
  selector:
    app: saga-orchestrator
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: saga-orchestrator-hpa
  namespace: ecommerce-prod
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: saga-orchestrator
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
