# Disable the built-in guest user so it cannot be used from any network address.
# The application user is created via RABBITMQ_DEFAULT_USER/RABBITMQ_DEFAULT_PASS
# environment variables in docker-compose.yml.
loopback_users.guest = false
